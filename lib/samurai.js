var fsTree = require('./fstree.js').create(),
	fsCache = require('./fscache.js').create(),
	tree = require('./tree.js').create();

function run() {
	// 1. Init FSTree
	// 2. Create empty Tree and FSCache.
	// 3. Init Tree and FSCache by resolvers.
	// 4. Run build.
};

console.log('samurai');